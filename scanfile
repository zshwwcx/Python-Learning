def scanfile(file,function):
  file=open(name,'r')
  while True:
    line=file.readline()
    if not
    if not line:
      break
    function(line)
  file.close()
  
  
/*  
def scanfile(file,function):
  for line in open(file,'r'):
    function(line)
*/
